Ler duas matrizes A e B do tipo vetor com 20 elementos. Construir uma matriz C, onde
cada elemento de C é a subtração do elemento correspondente de A com B

a=[]
b=[]
c=[]

for i in range(20):
	n=int(input('numero 1: '))
	a.append(n)
for i in range(20):
	n=int(input('numero 2: '))
	b.append(n)
for i in range(20):
	c.append(a[i]-b[i])
for i in c:
	print(i)
